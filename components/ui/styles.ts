// Shared Tailwind class-strings. These live under components/ (not lib/) so the
// tailwind `content` globs pick up the utility classes — lib/ is not scanned.

export const INPUT_CLS =
  'rounded-xl border border-neutral-300 px-4 py-3 text-base text-neutral-900 dark:border-neutral-700 dark:text-neutral-50';
